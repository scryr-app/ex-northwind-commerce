# Optional browser analytics. Leave the project token empty to disable.
# Copy to web/.env.local for local Vite development.
# Only use the public project token; never a personal PostHog API key.
VITE_POSTHOG_PROJECT_TOKEN=
VITE_POSTHOG_HOST=https://us.i.posthog.com
VITE_POSTHOG_ENVIRONMENT=local
